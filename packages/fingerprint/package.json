{
  "name": "@efforia/fingerprint",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "bin": { "efforia-fingerprint": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p .",
    "clean": "rimraf dist",
    "start": "node dist/index.js",
    "cli": "node dist/cli.js",
    "test": "npm run build && node --test test/fingerprint.test.cjs"
  },
  "dependencies": {
    "@efforia/shared": "file:../shared"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.3"
  }
}
