# Monad testnet deploy & anchor
# Copy to .env and set real values. Never commit .env.

# Wallet private key (hex, with 0x). Use a testnet-only wallet.
PRIVATE_KEY=0x...

# Optional: override RPC (default: https://testnet-rpc.monad.xyz)
# MONAD_RPC_URL=https://testnet-rpc.monad.xyz

# After deploy, set this to anchor certificates:
# CONTRACT_ADDRESS=0x...
